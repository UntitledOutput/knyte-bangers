name: Linux

on:
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: CD to makefile directory
      run: cd src

    - name: Install dependencies
      run: make
